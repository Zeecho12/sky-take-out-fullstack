services:
  redis:
    image: redis
    ports:
      - "6379:6379"

  backend:
    build: ./sky-backend
    ports:
      - "8080:8080"
    depends_on:
      - redis
    environment:
      SKY_REDIS_HOST: redis
      SKY_DATASOURCE_HOST: host.docker.internal
